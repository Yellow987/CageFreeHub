import { createTheme } from '@mui/system';

export const Theme = createTheme({
  palette: {
    primary: {
      main: '#3FAB94',
      contrastText: '#FFFFFF',
    },
    secondary: {
      main: '#FF0000',
    },
  },
});
